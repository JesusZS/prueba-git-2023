let n1 = 7;
let n2 = 3;


/**
 * 
 * @param {*} num1 
 * @param {*} num2 
 * @returns 
 */
function restar(num1, num2) {
    let resultado = num1 - num2;
    console.log(`el resultado de la resta es: ${resultado}`);

    return resultado;
}

restar(n1, n2);
